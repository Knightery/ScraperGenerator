{
    "builds": [{ 
        "src": "api/main.py", 
        "use": "@vercel/python",
        "config": {
            "includeFiles": "**"
        }
    }],
    "routes": [{ "src": "/(.*)", "dest": "api/main.py" }]
}